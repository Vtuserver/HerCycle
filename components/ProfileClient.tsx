"use client";
import {useEffect,useState} from "react"; import {signOut} from "next-auth/react";
export default function ProfileClient(){const [d,setD]=useState<any>(null),[msg,setMsg]=useState(""),[busy,setBusy]=useState(false);
useEffect(()=>{fetch("/api/data").then(r=>r.json()).then(setD)},[]);
if(!d)return <div className="page container">Loading profile...</div>;
const u=d.user;
async function save(e:any){e.preventDefault();setBusy(true);const f=new FormData(e.currentTarget);const b={type:"profile",name:f.get("name"),cycleLength:f.get("cycleLength"),periodLength:f.get("periodLength"),reminderEnabled:f.get("reminderEnabled")==="on",reminderEmail:f.get("reminderEmail")};const r=await fetch("/api/data",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(b)});setMsg(r.ok?"Profile saved.":"Could not save.");setBusy(false)}
return <main className="page"><div className="container" style={{maxWidth:720}}><h1>Profile</h1><p className="muted" style={{marginBottom:20}}>Adjust your cycle preferences.</p><div className="card"><form className="stack" onSubmit={save}><div><label>Name</label><input name="name" defaultValue={u.name}/></div><div className="formgrid"><div><label>Typical cycle length (days)</label><input name="cycleLength" type="number" min="15" max="60" defaultValue={u.cycleLength||28}/></div><div><label>Typical period length (days)</label><input name="periodLength" type="number" min="1" max="15" defaultValue={u.periodLength||5}/></div></div><div><label><input style={{width:"auto",marginRight:7}} type="checkbox" name="reminderEnabled" defaultChecked={u.reminderEnabled}/> Enable reminder preference</label></div><div><label>Reminder email</label><input name="reminderEmail" type="email" defaultValue={u.reminderEmail||u.email||""}/></div>{msg&&<div className="success">{msg}</div>}<button className="btn btn-primary" disabled={busy}>{busy?"Saving...":"Save settings"}</button></form><hr style={{border:0,borderTop:"1px solid var(--line)",margin:"24px 0"}}/><button className="btn btn-soft" onClick={()=>signOut({callbackUrl:"/"})}>Log out</button></div><div className="card" style={{marginTop:16}}><h3>Medical note</h3><p className="small muted">HerCycle provides tracking estimates, not diagnosis or contraception advice. If you have severe, unusual or persistent symptoms, contact a qualified healthcare professional.</p></div></div></main>}
